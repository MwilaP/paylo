import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export function PayrollGenerate() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Generate Payroll</CardTitle>
        <CardDescription>Process payroll for employees</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Payroll generation functionality will be implemented here.</p>
      </CardContent>
    </Card>
  )
}
